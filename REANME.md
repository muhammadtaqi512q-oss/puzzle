<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>MT OK</title> <style> html, body { margin: 0; padding: 0; width: 100%; height: 100%; overflow: hidden; background-color: #000000; /* Text selection disabled */ -webkit-user-select: none; -moz-user-select: none; -ms-user-select: none; user-select: none; } iframe { width: 100%; height: 100%; border: none; display: block; } /* Loader style jab tak URL fetch ho raha ho */ .loader { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); color: #ff007f; /* Neon pink touch */ font-family: sans-serif; font-size: 1.2rem; } </style> </head> <body> <div id="loading" class="loader">Loading secure environment...</div> <iframe id="myFrame" src="about:blank" allowfullscreen style="display: none;"></iframe> <script> // GitHub API Config const repoOwner = "muhammadtaqi512q-oss"; const repoName = "puzzle"; const apiUrl = `https://api.github.com/repos/${repoOwner}/${repoName}/readme`; async function fetchLinkFromReadme() { try { const response = await fetch(apiUrl); if (!response.ok) throw new Error("Failed to fetch README"); const data = await response.json(); // GitHub API README ka content Base64 mein deta hai, usko decode karna parta hai const decodedContent = atob(data.content); // RegEx ke zariye URL dhoondna (chahe plain text ho ya markdown [link](url) format mein) const urlRegex = /(https?:\/\/[^\s]+)/g; const matches = decodedContent.match(urlRegex); if (matches && matches.length > 0) { let targetUrl = matches[0].trim(); // Agar markdown format ka ")" end mein aa jaye toh usko saaf karne ke liye if (targetUrl.endsWith(')')) { targetUrl = targetUrl.slice(0, -1); } // Iframe mein URL set karna const iframe = document.getElementById('myFrame'); iframe.src = targetUrl; // Loader chupana aur iframe dikhana document.getElementById('loading').style.display = 'none'; iframe.style.display = 'block'; } else { document.getElementById('loading').innerText = "Error: No URL found in README."; } } catch (error) { console.error("Error fetching data:", error); document.getElementById('loading').innerText = "Error loading configuration."; } } // Code Protection Code (Jo aapne pehle lagaya tha) function protectPage(win) { win.document.addEventListener('contextmenu', function(e) { e.preventDefault(); }); win.document.addEventListener('keydown', function(e) { if (e.key === 'F12' || e.keyCode === 123) { e.preventDefault(); return false; } if (e.ctrlKey && ( e.key === 'u' || e.key === 'U' || e.key === 's' || e.key === 'S' || e.shiftKey && (e.key === 'i' || e.key === 'I' || e.key === 'j' || e.key === 'J' || e.key === 'c' || e.key === 'C') )) { e.preventDefault(); return false; } if (e.metaKey && e.altKey && (e.key === 'i' || e.key === 'I' || e.key === 'u' || e.key === 'U')) { e.preventDefault(); return false; } }); } window.onload = function() { fetchLinkFromReadme(); protectPage(window); }; </script> </body> </html>
